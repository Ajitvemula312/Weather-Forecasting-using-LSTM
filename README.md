# Weather Forecasting with LSTM + Feature Fusion

This project demonstrates a complete LSTM forecasting pipeline with synthetic multi-year weather data,
feature engineering, model training, evaluation and basic predict script.

## Setup

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
